package crosemont.dti.g66.service_gestion_stage.Services

import crosemont.dti.g66.service_gestion_stage.DAO.AdresseDAO
import crosemont.dti.g66.service_gestion_stage.Modèle.Adresse
import java.util.*
import kotlin.jvm.optionals.toList

class AdresseService(val db: AdresseDAO) {
  /*  fun chercherTousLesAdresses(): List<Adresse> = db.findAll().toList()

    fun chercherAdresseParId(id:String): List<Adresse> = db.findById(id).toList()

    fun ajouterAdresse(adresse: Adresse) = db.save(adresse)

    fun <T : Any> Optional<out T>.toList(): List<T> =
        if (isPresent) listOf(get()) else emptyList()*/

}